n = input("Введіть чотиризначне число: ")

while len(n) != 4 or not n.isdigit():  
    n = input("Будь ласка, введіть чотиризначне число: ")

count = 0
i = 0

while i < len(n):
    if n[i] == '7':
        count += 1
    i += 1

print(f"Цифра 7 зустрічається {count} разів у числі {n}.")
